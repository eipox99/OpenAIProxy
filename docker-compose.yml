services:
  openaiproxy:
    image: ghcr.io/eipox99/openaiproxy:latest
    container_name: openaiproxy
    ports:
      - "8000:8000"
    volumes:
      - /mnt/user/appdata/OpenAIProxy/data:/app/data
      - /mnt/user/appdata/OpenAIProxy/.env:/app/.env:ro
    restart: unless-stopped
